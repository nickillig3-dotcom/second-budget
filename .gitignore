.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
*.duckdb
*.duckdb.wal
data/cache/
data/qc/

# The eCFR source IS committed -- it is public-domain federal text and it is
# what makes the quotation tests runnable on a fresh clone. Only the index
# built from it is derived, so only that is ignored.
data/corpus/*.sqlite
data/corpus/techdoc.txt
data/corpus/*.pdf

notes/scratch/

# Never. Credentials live outside the repo, in zugang/, and are read from the
# environment. Nothing key-shaped is allowed to become a tracked file.
*.key
.bedrock-key
.gh-token
*credentials*

# working files, not artefacts
.second-budget-case.json
request.md
